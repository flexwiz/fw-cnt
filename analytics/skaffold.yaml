apiVersion: skaffold/v4beta11
kind: Config
metadata:
  name: analytics

requires:
  - configs: ["superset"]
    path: superset
    activeProfiles: &profiles
      - name: minikube
        activatedBy: [minikube]
      - name: gcp
        activatedBy: [gcp]

  # - configs: ["other"]
  #   path: other
  #   activeProfiles: *profiles
