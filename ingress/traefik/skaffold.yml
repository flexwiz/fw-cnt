apiVersion: skaffold/v4beta11
kind: Config
metadata:
  name: traefik

profiles:
  - name: minikube
    activation:
      - kubeContext: minikube
    deploy:
      kubectl: {}
    manifests:
      rawYaml:
        - ./k8s/*.yml
    portForward:
      - resourceType: service
        resourceName: traefik
        port: 8080
        localPort: 80
