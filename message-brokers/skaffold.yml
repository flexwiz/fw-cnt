apiVersion: skaffold/v4beta11
kind: Config
metadata:
  name: message-brokers

requires:
  - configs: ["rabbitmq"]
    path: rabbitmq
    activeProfiles: &profiles
      - name: minikube
        activatedBy: [minikube]

  # - configs: ["kafka"]
  #   path: kafka
  #   activeProfiles: *profiles
