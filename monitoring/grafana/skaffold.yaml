apiVersion: skaffold/v4beta11
kind: Config
metadata:
  name: grafana

requires:
  - configs: ["grafana"]
    path: grafana
    activeProfiles: &profiles
      - name: minikube
        activatedBy: [minikube]
      - name: gcp
        activatedBy: [gcp]

  # - configs: ["other"]
  #   path: other
  #   activeProfiles: *profiles
