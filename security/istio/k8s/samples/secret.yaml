# Secret for mutual TLS between services (example)
apiVersion: v1
kind: Secret
metadata:
  name: istio-ca-secret
  namespace: istio-system
type: Opaque
data:
  ca-cert.pem: ""  # Replace with base64-encoded CA certificate
  ca-key.pem: ""   # Replace with base64-encoded CA private key
