# overlays/development/kustomization.yaml
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: fw-security

labels:
- includeSelectors: true
  pairs:
    flexwiz.io/env: development

resources:
- ../../base
- sealed-secrets.yaml

patches:
- patches/deployment-patch.yaml
- patches/configmap-patch.yaml
- patches/ingress-patch.yaml

configMapGenerator:
- name: keycloak-config
  behavior: merge
  literals:
  - KC_HOSTNAME=keycloak.dev.example.com

# secretGenerator:
# - name: metabase-db-credentials
#   envs:
#   - secrets/db-credentials.env
#   type: Opaque
